view: table
beanClass: com.dynamia.cms.site.menus.domain.MenuItem
hidden: [id,menu,parentItem,styleClass,order]
sortFields: [name,title,type,icon,href,target,page,page.category]

fields:
    page.category:
        label: Category

        
params:
    orderBy: order
